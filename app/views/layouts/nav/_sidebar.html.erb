<nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
      <div class="sidebar-sticky pt-3">
        <ul class="nav flex-column">
          <%
=begin%>
 <%= render "properties/#{ current_account.admin? ? 'admin' : 'profile_contact_details' }" if account_signed_in? %> 
<%
=end%>
          <div class="col-lg-10">
            <button class="btn btn-block btn-info mb-1">
              <%= link_to 'New Match', new_property_path, :style=>"color:#fff;" %>
            </button>
          </div>
          <div class="col-lg-10">
            <button class="btn btn-block btn-success mb-1">
              <%= link_to 'ブログ更新', new_post_path, :style=>"color:#fff;" %>
            </button>
          </div>
        </ul>

        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
          <span>設定</span>
        </h6>
        <ul class="nav flex-column mb-2">
          <li class="nav-item">
            <%= link_to profile_path(current_account), class: "nav-link" do %>
              <i class="far fa-file-alt"></i>
              プロフィール
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to edit_account_registration_path, class: "nav-link" do %> 
              <i class="fas fa-chart-bar"></i>
                アカウント設定        
            <% end %> 
          </li>
        </ul>
      </div>
    </nav>